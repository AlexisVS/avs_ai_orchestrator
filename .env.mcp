# MCP Docker Configuration Environment Variables

# GitHub Configuration
GITHUB_TOKEN=your_github_token_here
GITHUB_OWNER=AlexisVS
GITHUB_REPO=avs_ai_orchestrator

# Project Paths
PROJECT_ROOT=/workspace
HOST_PROJECT_PATH=C:/Users/alexi/mcp-agent/avs_ai_orchestrator

# Docker Network Configuration
MCP_NETWORK=mcp-network

# API Keys (if needed)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
NEEDLE_API_KEY=optional-needle-api-key

# Local Model Configuration
LM_STUDIO_URL=http://host.docker.internal:1234
LM_STUDIO_MODEL=qwen/qwen3-coder-30b

# MCP Server Ports (if using host networking)
MCP_FILESYSTEM_PORT=8001
MCP_GIT_PORT=8002
MCP_PYTHON_PORT=8003
MCP_MEMORY_PORT=8004